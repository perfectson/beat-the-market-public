name: myenv
channels:
  - conda-forge
dependencies:
  - python=3.5

tasks:
  - init: conda install -c Quantopian zipline
    command: echo "Replace me with something that should run on every start, or just
      remove me entirely."
